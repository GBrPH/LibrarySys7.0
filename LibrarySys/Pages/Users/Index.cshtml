@page
@using Microsoft.AspNetCore.Authorization
@model UsersModel
@attribute [Authorize(Roles = "Librarian")]

<h2>Registered Users</h2>

<table>
    <thead>
        <tr>
            <th>Username</th>
            <th>Full Name</th>
            <th>Role</th>
            <th>Status</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model.Users)
        {
            <tr>
                <td>@user.Username</td>
                <td>@user.FullName</td>
                <td>@user.Role</td>
                <td>@(user.IsLoggedIn ? "Online" : "Offline")</td>
            </tr>
        }
    </tbody>
</table>
